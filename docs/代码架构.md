# OmniAgent AI Agent 开发教学平台 - 完整项目代码架构

> **版本**: v1.0  
> **创建日期**: 2026-05-14  
> **项目类型**: 教学项目 / AI Agent 平台  
> **技术栈**: FastAPI + Vue 3 + LangChain  
> **当前版本**: v2.4.0

---

## 项目总览

```
OmniAgent/                              # 项目根目录
├── src/                                # 源代码目录
│   ├── frontend/                       # 前端项目（Vue 3）
│   └── backend/                        # 后端项目（FastAPI）
├── docs/                               # 文档目录
│   ├── 00-mrd.md                       # 市场需求文档
│   ├── 01-prd.md                       # 产品需求文档
│   ├── 03-技术方案文档.md              # 技术方案文档
│   ├── 技术选型.md                     # 技术选型文档
│   ├── 代码架构.md                     # 代码架构文档
│   └── Agent项目分析.md                # 项目分析文档
├── scripts/                            # 脚本目录
│   └── start.py                        # 一键启动脚本
├── tests/                              # 测试目录
├── .gitignore
├── .env.example                        # 环境变量示例
├── config.yaml                         # 主配置文件
├── pyproject.toml                       # Python依赖管理
├── requirements.txt                    # Python依赖列表
└── README.md                           # 项目说明
```

---

## 1. 前端项目结构

### 1.1 完整目录树

```
src/frontend/
├── public/                             # 静态资源
│   ├── favicon.ico
│   └── logo.png
│
├── src/
│   ├── api/                            # API接口层
│   │   ├── index.ts                    # API统一导出
│   │   ├── request.ts                  # Axios封装（拦截器、错误处理）
│   │   ├── chat.ts                     # 对话相关接口
│   │   ├── agent.ts                    # Agent管理接口
│   │   ├── knowledge.ts                # 知识库接口
│   │   ├── user.ts                     # 用户相关接口
│   │   └── tool.ts                     # 工具管理接口
│   │
│   ├── assets/                         # 资源文件
│   │   ├── images/                     # 图片资源
│   │   ├── styles/                     # 全局样式
│   │   │   ├── reset.scss              # 样式重置
│   │   │   ├── variables.scss          # 样式变量
│   │   │   └── common.scss             # 通用样式
│   │   └── icons/                      # 图标资源
│   │
│   ├── components/                     # 公共组件
│   │   ├── ChatPanel/                  # 聊天面板组件
│   │   │   ├── index.vue               # 组件主文件
│   │   │   └── style.scss              # 组件样式
│   │   ├── AgentCard/                  # Agent卡片组件
│   │   ├── MessageList/                # 消息列表组件
│   │   ├── MessageItem/                # 消息项组件
│   │   ├── ToolCallDisplay/            # 工具调用展示组件
│   │   ├── FlowChart/                  # 流程图组件
│   │   ├── Loading/                    # 加载组件
│   │   └── Empty/                      # 空状态组件
│   │
│   ├── pages/                          # 页面组件
│   │   ├── Chat/                       # 对话页面
│   │   │   ├── index.vue               # 页面主文件
│   │   │   ├── components/             # 页面私有组件
│   │   │   │   ├── InputArea.vue       # 输入区域组件
│   │   │   │   ├── MessageList.vue     # 消息列表组件
│   │   │   │   └── AgentSelector.vue   # Agent选择器组件
│   │   │   └── style.scss              # 页面样式
│   │   ├── AgentManage/                # Agent管理页面
│   │   │   ├── index.vue
│   │   │   └── components/
│   │   │       ├── AgentForm.vue       # Agent表单组件
│   │   │       └── ToolList.vue        # 工具列表组件
│   │   ├── Knowledge/                  # 知识库管理页面
│   │   │   ├── index.vue
│   │   │   └── components/
│   │   │       ├── FileUpload.vue      # 文件上传组件
│   │   │       └── ChunkList.vue       # 分块列表组件
│   │   ├── Dashboard/                  # 数据看板页面
│   │   │   ├── index.vue
│   │   │   └── components/
│   │   │       ├── StatCard.vue        # 统计卡片组件
│   │   │       └── Chart.vue           # 图表组件
│   │   ├── Login/                      # 登录页面
│   │   │   └── index.vue
│   │   └── ErrorPage/                  # 错误页面
│   │       └── index.vue
│   │
│   ├── stores/                         # 状态管理（Pinia）
│   │   ├── index.ts                    # Store统一导出
│   │   ├── chat.ts                     # 聊天状态管理
│   │   ├── user.ts                     # 用户状态管理
│   │   ├── agent.ts                    # Agent状态管理
│   │   └── knowledge.ts                # 知识库状态管理
│   │
│   ├── router/                         # 路由配置
│   │   ├── index.ts                    # 路由主文件
│   │   └── routes.ts                   # 路由表
│   │
│   ├── utils/                          # 工具函数
│   │   ├── format.ts                   # 格式化工具
│   │   ├── validate.ts                 # 验证工具
│   │   ├── storage.ts                  # 本地存储工具
│   │   └── sse.ts                      # SSE事件处理工具
│   │
│   ├── composables/                    # 组合式函数
│   │   ├── useChat.ts                  # 聊天相关Hook
│   │   ├── useAgent.ts                 # Agent相关Hook
│   │   └── useSSE.ts                   # SSE相关Hook
│   │
│   ├── types/                          # 类型定义
│   │   ├── api.d.ts                    # API类型定义
│   │   ├── chat.d.ts                   # 聊天类型定义
│   │   ├── agent.d.ts                  # Agent类型定义
│   │   └── knowledge.d.ts              # 知识库类型定义
│   │
│   ├── constants/                      # 常量定义
│   │   ├── index.ts                    # 常量统一导出
│   │   ├── api.ts                      # API常量
│   │   └── config.ts                   # 配置常量
│   │
│   ├── App.vue                         # 根组件
│   └── main.ts                         # 入口文件
│
├── .env.development                    # 开发环境变量
├── .env.production                     # 生产环境变量
├── .eslintrc.cjs                       # ESLint配置
├── .prettierrc                         # Prettier配置
├── .gitignore
├── index.html                          # HTML模板
├── package.json                        # 依赖配置
├── pnpm-lock.yaml                      # 依赖锁定文件
├── tsconfig.json                       # TypeScript配置
├── vite.config.ts                      # Vite配置
└── README.md                           # 项目说明
```

### 1.2 package.json 示例

```json
{
  "name": "omniagent-frontend",
  "version": "2.4.0",
  "description": "OmniAgent AI Agent 开发教学平台 - 前端项目",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts --fix --ignore-path .gitignore",
    "format": "prettier --write src/"
  },
  "dependencies": {
    "vue": "^3.4.21",
    "vue-router": "^4.3.0",
    "pinia": "^2.1.7",
    "pinia-plugin-persistedstate": "^3.2.1",
    "axios": "^1.6.8",
    "element-plus": "^2.7.0",
    "@element-plus/icons-vue": "^2.3.1",
    "echarts": "^5.5.1",
    "monaco-editor": "^0.47.0",
    "md-editor-v3": "^4.20.0",
    "vditor": "^3.9.9"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.2.8",
    "typescript": "^5.4.3",
    "vue-tsc": "^2.0.14",
    "sass": "^1.75.0",
    "eslint": "^8.57.0",
    "eslint-plugin-vue": "^9.25.0",
    "@typescript-eslint/eslint-plugin": "^7.8.0",
    "@typescript-eslint/parser": "^7.8.0",
    "prettier": "^3.2.5",
    "unplugin-auto-import": "^0.17.5",
    "unplugin-vue-components": "^0.27.0"
  }
}
```

### 1.3 核心文件说明

| 文件路径 | 功能说明 |
|----------|----------|
| `src/main.ts` | 应用入口，初始化Vue应用、插件、路由 |
| `src/App.vue` | 根组件，应用布局 |
| `src/router/index.ts` | 路由配置，路由守卫 |
| `src/api/request.ts` | Axios封装，拦截器配置 |
| `src/stores/chat.ts` | 聊天状态管理，消息历史 |
| `src/composables/useSSE.ts` | SSE事件处理Hook |
| `vite.config.ts` | Vite构建配置，代理配置 |
| `tsconfig.json` | TypeScript配置 |

---

## 2. 后端项目结构

### 2.1 完整目录树

```
src/backend/
├── agentchat/                         # 源代码主目录
│   ├── main.py                        # 应用入口
│   ├── settings.py                    # 配置管理
│   │
│   ├── api/                           # API路由层
│   │   ├── __init__.py
│   │   ├── deps.py                    # 依赖注入
│   │   ├── errcode/                   # 错误码定义
│   │   │   ├── __init__.py
│   │   │   └── exception.py           # 异常定义
│   │   │
│   │   └── v1/                        # v1版本API
│   │       ├── __init__.py
│   │       ├── auth.py                # 认证接口
│   │       ├── completion.py          # 对话接口
│   │       ├── dialog.py              # 对话管理接口
│   │       ├── agent.py               # Agent管理接口
│   │       ├── knowledge.py           # 知识库接口
│   │       ├── tool.py                # 工具管理接口
│   │       └── mcp_server.py          # MCP服务器接口
│   │
│   ├── core/                          # 核心模块
│   │   ├── __init__.py
│   │   ├── agents/                    # Agent实现
│   │   │   ├── __init__.py
│   │   │   ├── base_agent.py          # Agent基类
│   │   │   ├── general_agent.py       # 通用Agent
│   │   │   ├── react_agent.py         # ReAct Agent
│   │   │   ├── plan_agent.py          # Plan-Execute Agent
│   │   │   ├── mcp_agent.py           # MCP Agent
│   │   │   ├── skill_agent.py         # Skill Agent
│   │   │   ├── text2sql_agent.py      # Text2SQL Agent
│   │   │   └── codeact_agent.py       # CodeAct Agent
│   │   │
│   │   ├── callbacks/                 # 回调处理
│   │   │   ├── __init__.py
│   │   │   ├── base_callback.py       # 基础回调
│   │   │   └── streaming_callback.py  # 流式回调
│   │   │
│   │   └── models/                    # 模型管理
│   │       ├── __init__.py
│   │       ├── llm.py                 # LLM模型配置
│   │       └── embedding.py           # Embedding模型配置
│   │
│   ├── database/                      # 数据库层
│   │   ├── __init__.py
│   │   ├── dao/                       # 数据访问对象
│   │   │   ├── __init__.py
│   │   │   ├── base.py                # 基础DAO
│   │   │   ├── user.py                # 用户DAO
│   │   │   ├── agent.py               # Agent DAO
│   │   │   ├── dialog.py              # 对话DAO
│   │   │   └── knowledge.py           # 知识库DAO
│   │   │
│   │   ├── models/                    # 数据模型
│   │   │   ├── __init__.py
│   │   │   ├── user.py                # 用户模型
│   │   │   ├── agent.py               # Agent模型
│   │   │   ├── dialog.py              # 对话模型
│   │   │   ├── message.py             # 消息模型
│   │   │   ├── knowledge.py           # 知识库模型
│   │   │   └── document.py            # 文档模型
│   │   │
│   │   └── init_db.py                 # 数据库初始化
│   │
│   ├── services/                      # 业务服务层
│   │   ├── __init__.py
│   │   │
│   │   ├── rag/                       # RAG服务
│   │   │   ├── __init__.py
│   │   │   ├── retrieval.py           # 检索服务
│   │   │   ├── parser.py              # 文档解析
│   │   │   ├── chunking.py            # 分块策略
│   │   │   ├── handler.py             # RAG处理器
│   │   │   └── es_client.py           # Elasticsearch客户端
│   │   │
│   │   ├── mcp/                       # MCP服务
│   │   │   ├── __init__.py
│   │   │   ├── manager.py             # MCP管理器
│   │   │   └── client.py              # MCP客户端
│   │   │
│   │   ├── memory/                    # 记忆服务
│   │   │   ├── __init__.py
│   │   │   ├── base_memory.py         # 基础记忆
│   │   │   ├── short_memory.py        # 短期记忆
│   │   │   └── long_memory.py         # 长期记忆
│   │   │
│   │   ├── storage/                   # 存储服务
│   │   │   ├── __init__.py
│   │   │   ├── base_storage.py        # 基础存储
│   │   │   ├── minio.py               # MinIO存储
│   │   │   └── oss.py                 # OSS存储
│   │   │
│   │   ├── chat_service.py            # 聊天服务
│   │   ├── agent_service.py           # Agent服务
│   │   └── knowledge_service.py       # 知识库服务
│   │
│   ├── tools/                         # 工具集成
│   │   ├── __init__.py
│   │   ├── builtin/                   # 内置工具
│   │   │   ├── __init__.py
│   │   │   ├── search.py              # 搜索工具
│   │   │   ├── calculator.py          # 计算器工具
│   │   │   ├── weather.py             # 天气工具
│   │   │   └── time.py                # 时间工具
│   │   └── custom/                    # 自定义工具
│   │
│   ├── middleware/                    # 中间件
│   │   ├── __init__.py
│   │   ├── auth.py                    # 认证中间件
│   │   ├── cors.py                    # CORS中间件
│   │   └── logging.py                 # 日志中间件
│   │
│   ├── prompts/                       # 提示词模板
│   │   ├── __init__.py
│   │   ├── agent_prompts.py           # Agent提示词
│   │   └── skill_prompts.py           # Skill提示词
│   │
│   ├── schema/                        # 数据结构定义
│   │   ├── __init__.py
│   │   ├── common.py                  # 通用Schema
│   │   ├── agent.py                   # Agent Schema
│   │   ├── chat.py                    # 聊天 Schema
│   │   └── knowledge.py               # 知识库 Schema
│   │
│   └── utils/                         # 工具函数
│       ├── __init__.py
│       ├── logger.py                  # 日志工具
│       ├── datetime.py                # 时间工具
│       └── security.py                # 安全工具
│
├── config.yaml                        # 主配置文件
├── tests/                             # 测试目录
│   ├── __init__.py
│   ├── conftest.py                    # 测试配置
│   ├── test_chat.py                   # 聊天测试
│   ├── test_agent.py                  # Agent测试
│   ├── test_rag.py                    # RAG测试
│   └── test_tools.py                  # 工具测试
│
├── scripts/                           # 脚本目录
│   ├── init_db.py                     # 数据库初始化脚本
│   └── migrate.py                     # 数据迁移脚本
│
├── logs/                              # 日志目录
│   └── .gitkeep
│
├── .env                               # 环境变量
├── .env.example                       # 环境变量示例
├── .gitignore
├── pyproject.toml                     # Poetry依赖管理
├── requirements.txt                   # 依赖列表
└── README.md                          # 项目说明
```

### 2.2 依赖配置文件

**pyproject.toml (Poetry)**
```toml
[tool.poetry]
name = "omniagent-backend"
version = "2.4.0"
description = "OmniAgent AI Agent 开发教学平台 - 后端项目"
authors = ["dtsola"]

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.121.0"
uvicorn = {extras = ["standard"], version = "^0.29.0"}
sqlmodel = "^0.0.27"
langchain = "^1.0.3"
langchain-community = "^0.4.1"
langchain-openai = "^0.1.8"
redis = "^7.0.1"
elasticsearch = "^9.2.0"
anthropic = "^0.72.0"
dashscope = "^1.20.0"
loguru = "^0.7.2"

[tool.poetry.dev-dependencies]
pytest = "^8.1.1"
pytest-asyncio = "^0.23.6"
httpx = "^0.27.0"
```

**requirements.txt**
```txt
# Web框架
fastapi==0.121.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# 数据库
sqlmodel==0.0.27
aiomysql==0.2.0
pymysql==1.1.0
redis==7.0.1

# AI框架
langchain==1.0.3
langchain-community==0.4.1
langchain-openai==0.1.8
anthropic==0.72.0

# 搜索引擎
elasticsearch==9.2.0

# 其他依赖
pydantic==2.7.1
pyyaml==6.0.1
loguru==0.7.2
python-dotenv==1.0.1
```

### 2.3 核心文件说明

| 文件路径 | 功能说明 |
|----------|----------|
| `agentchat/main.py` | FastAPI应用入口，应用创建和启动 |
| `agentchat/settings.py` | 配置管理，加载YAML配置 |
| `config.yaml` | 主配置文件 |
| `agentchat/core/agents/base_agent.py` | Agent基类，定义Agent接口 |
| `agentchat/core/agents/react_agent.py` | ReAct Agent实现 |
| `agentchat/services/rag/retrieval.py` | RAG检索服务 |
| `agentchat/services/mcp/manager.py` | MCP服务器管理 |
| `agentchat/api/v1/completion.py` | 对话接口实现 |

---

## 3. 环境变量配置

### 3.1 前端环境变量

**.env.development**
```env
# API配置
VITE_API_BASE_URL=http://localhost:7860/api/v1
VITE_WS_BASE_URL=ws://localhost:7860

# 应用配置
VITE_APP_NAME=OmniAgent
VITE_APP_VERSION=2.4.0

# 功能开关
VITE_ENABLE_MOCK=false
VITE_ENABLE_DEBUG=true
```

**.env.production**
```env
# API配置
VITE_API_BASE_URL=/api/v1
VITE_WS_BASE_URL=ws://your-domain.com

# 应用配置
VITE_APP_NAME=OmniAgent
VITE_APP_VERSION=2.4.0

# 功能开关
VITE_ENABLE_MOCK=false
VITE_ENABLE_DEBUG=false
```

### 3.2 后端环境变量

**.env.example**
```env
# 应用配置
APP_NAME=OmniAgent
APP_ENV=development
DEBUG=true
SERVER_HOST=0.0.0.0
SERVER_PORT=7860

# 数据库配置
DATABASE_URL=mysql+pymysql://<user>:<password>@localhost:3306/agentchat
ASYNC_DATABASE_URL=mysql+aiomysql://<user>:<password>@localhost:3306/agentchat

# Redis配置
REDIS_URL=redis://localhost:6379/0

# 安全配置
SECRET_KEY=your-secret-key-here
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080

# AI模型配置
OPENAI_API_KEY=sk-xxx
ANTHROPIC_API_KEY=sk-ant-xxx
DASHSCOPE_API_KEY=sk-xxx

# 向量数据库配置
VECTOR_DB_TYPE=chroma
CHROMA_PERSIST_DIRECTORY=./data/chroma
MILVUS_HOST=localhost
MILVUS_PORT=19530

# 存储配置
STORAGE_MODE=minio
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=agentchat

# Elasticsearch配置（可选）
ENABLE_ELASTICSEARCH=false
ES_HOST=http://localhost:9200

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=logs/app.log
```

### 3.3 配置文件管理

项目支持本地配置文件覆盖，方便不同环境的配置管理：

#### 配置加载优先级

**优先级顺序**：
1. **config.local.yaml** - 本地配置（优先，如果存在）
2. **config.yaml** - 默认配置（fallback）

**加载逻辑**：
```
尝试加载 config.local.yaml
    ↓
存在？ → 使用本地配置 ✅
    ↓ 不存在
加载 config.yaml ✅
```

#### 使用方式

**步骤 1**: 复制默认配置作为模板
```bash
cd src/backend/agentchat
cp config.yaml config.local.yaml
```

**步骤 2**: 修改本地配置文件
```yaml
# config.local.yaml - 完整的配置文件
server:
  project_name: "OmniAgent-Dev"
  debug: true

mysql:
  endpoint: "mysql+pymysql://<user>:<password>@localhost:3306/omniagent_dev"

multi_models:
  default_model: "qwen"
  qwen:
    api_key: "sk-your-dev-api-key"
```

**步骤 3**: 启动应用（自动使用本地配置）
```bash
python -m uvicorn agentchat.main:app --reload
```

#### 典型使用场景

| 场景 | config.yaml | config.local.yaml |
|------|-------------|-------------------|
| 生产环境 | 完整生产配置 | 不存在 ✅ |
| 开发环境 | 完整默认配置 | 完整开发配置 ✅ |
| 团队协作 | 提交到 Git | .gitignore 忽略 |

#### 重要说明

⚠️ **完全替代**: `config.local.yaml` 是**完整配置文件**，不是部分覆盖
✅ **安全性**: 本地配置已加入 `.gitignore`，不会提交敏感信息
📝 **模板**: 参考 `config.local.yaml.example` 了解常用配置项

#### 配置对比

| 特性 | config.yaml | config.local.yaml |
|------|-------------|-------------------|
| 提交到 Git | ✅ 是 | ❌ 否 |
| 优先级 | 低 | 高 |
| 典型用途 | 生产/默认配置 | 开发/个人配置 |
| 必须完整 | ✅ 是 | ✅ 是 |

---

## 4. 项目启动说明

### 4.1 前端项目启动

```bash
# 进入前端项目目录
cd src/frontend

# 安装依赖（推荐使用pnpm）
pnpm install
# 或使用npm
npm install

# 启动开发服务器
pnpm run dev
# 或使用npm
npm run dev

# 构建生产版本
pnpm run build

# 预览生产构建
pnpm run preview

# 代码检查
pnpm run lint

# 代码格式化
pnpm run format
```

### 4.2 后端项目启动

```bash
# 进入后端项目目录
cd src/backend

# 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
# 或使用Poetry
poetry install

# 初始化数据库
python -m agentchat.database.init_db

# 启动开发服务器
python -m uvicorn agentchat.main:app --host 0.0.0.0 --port 7860 --reload

# 或使用启动脚本
python scripts/start.py
```

### 4.3 Docker部署

```bash
# 构建并启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down

# 重启服务
docker-compose restart
```

---

## 5. 代码规范

### 5.1 通用规范

- **文件命名**：
  - Python: `snake_case.py`
  - Vue/TS: `PascalCase.vue` / `PascalCase.ts`
  - 配置文件: `kebab-case.*`

- **目录结构**：
  - 按功能模块划分目录
  - 相关文件放在同一目录下
  - 公共代码放在共享目录

- **代码风格**：
  - 使用格式化工具统一风格（Prettier/Black）
  - 使用代码检查工具保证质量（ESLint/Ruff）
  - 编写清晰的注释和文档字符串

### 5.2 前端代码规范

- **命名约定**：
  - 组件：`PascalCase` (如 `UserList.vue`)
  - 变量/函数：`camelCase` (如 `userName`)
  - 常量：`UPPER_SNAKE_CASE` (如 `API_BASE_URL`)
  - 文件：组件`PascalCase.vue`，工具`camelCase.ts`

- **目录约定**：
  - 组件目录包含主文件和样式文件
  - 页面目录包含主文件、私有组件和样式文件
  - 公共组件放在 `components/` 目录
  - 页面组件放在 `pages/` 目录

- **代码组织**：
  ```vue
  <template>
    <!-- 模板 -->
  </template>

  <script setup lang="ts">
  // 导入
  // 定义
  // 方法
  // 生命周期
  </script>

  <style scoped lang="scss">
  // 样式
  </style>
  ```

### 5.3 后端代码规范

- **命名约定**：
  - 类：`PascalCase` (如 `UserService`)
  - 函数/变量：`snake_case` (如 `get_user`)
  - 常量：`UPPER_SNAKE_CASE` (如 `MAX_RETRIES`)
  - 文件：`snake_case.py`

- **目录约定**：
  - 按功能模块组织代码
  - 每个模块包含 `__init__.py`
  - 相关功能放在同一目录下

- **代码组织**：
  ```python
  # 导入顺序：标准库 -> 第三方库 -> 本地模块
  import os
  from typing import Optional
  
  from fastapi import Depends
  from sqlmodel import Session
  
  from agentchat.core import something
  from agentchat.api import deps

  # 类/函数定义
  class MyClass:
      """类的文档字符串"""
      def __init__(self):
          pass
      
      def my_method(self):
          """方法的文档字符串"""
          pass
  ```

---

## 6. 常用命令

### 6.1 前端常用命令

```bash
# 开发
pnpm run dev              # 启动开发服务器
pnpm run build            # 构建生产版本
pnpm run preview          # 预览生产构建

# 代码质量
pnpm run lint             # 代码检查
pnpm run format           # 代码格式化
pnpm run type-check       # 类型检查

# 测试
pnpm run test             # 运行测试
pnpm run test:coverage    # 测试覆盖率
```

### 6.2 后端常用命令

```bash
# 开发
uvicorn agentchat.main:app --reload    # 启动开发服务器
python scripts/start.py                # 一键启动脚本

# 数据库
python -m agentchat.database.init_db   # 初始化数据库
alembic init alembic                   # 初始化Alembic
alembic revision --autogenerate -m "message"  # 创建迁移
alembic upgrade head                    # 应用迁移
alembic downgrade -1                    # 回滚迁移

# 测试
pytest                                 # 运行测试
pytest tests/test_agent.py             # 运行特定测试
pytest --cov=agentchat                 # 测试覆盖率

# 代码质量
ruff check .                           # 代码检查
black .                                # 代码格式化
mypy agentchat/                        # 类型检查
```

---

## 7. 技术栈说明

### 7.1 前端技术栈

| 类型 | 技术选型 | 说明 |
|------|---------|------|
| **核心框架** | Vue 3.4+ | 组合式API、性能优秀、学习曲线平缓 |
| **构建工具** | Vite 5.2+ | 开发服务器启动快、HMR实时更新 |
| **UI组件库** | Element Plus 2.7+ | 组件丰富、设计优秀、TypeScript支持 |
| **状态管理** | Pinia | Vue 3官方推荐、轻量级、支持持久化 |
| **路由管理** | Vue Router 4+ | Vue官方路由器、动态路由支持 |
| **HTTP客户端** | Axios | Promise-based、拦截器支持 |
| **类型检查** | TypeScript 5.4+ | 类型安全、IDE智能提示 |
| **代码规范** | ESLint + Prettier | 代码检查和格式化 |

### 7.2 后端技术栈

| 类型 | 技术选型 | 说明 |
|------|---------|------|
| **Web框架** | FastAPI 0.121+ | 现代化、异步支持、自动API文档 |
| **应用服务器** | Uvicorn | ASGI服务器、高性能 |
| **数据库** | MySQL 8.0+ | 关系型数据库、事务支持 |
| **ORM** | SQLModel 0.0.27+ | 基于Pydantic、类型安全、异步支持 |
| **缓存** | Redis 7.0+ | 高性能键值存储 |
| **AI框架** | LangChain 1.0+ | AI应用开发标准框架 |
| **日志** | Loguru | 简洁易用的日志库 |
| **代码规范** | Ruff + Black | 快速的Python linter和格式化 |

---

## 8. 6层架构映射

项目代码结构与6层架构的对应关系：

```
用户层 (第6层)
├── src/frontend/src/pages/          # 页面组件
└── src/frontend/src/components/     # 公共组件

对话层 (第5层)
├── src/backend/agentchat/api/v1/completion.py    # 对话接口
├── src/backend/agentchat/services/memory/        # 记忆服务
└── src/frontend/src/stores/chat.ts               # 对话状态

Agent决策层 (第4层)
├── src/backend/agentchat/core/agents/           # Agent实现
└── src/backend/agentchat/services/chat_service.py # Agent服务

能力层 (第3层)
├── src/backend/agentchat/services/rag/          # RAG服务
├── src/backend/agentchat/services/mcp/          # MCP服务
├── src/backend/agentchat/services/storage/      # 存储服务
└── src/backend/agentchat/tools/                 # 工具集成

模型层 (第2层)
├── src/backend/agentchat/core/models/           # 模型配置
└── src/backend/config.yaml                       # 模型配置

数据层 (第1层)
├── src/backend/agentchat/database/              # 数据库层
├── src/backend/agentchat/services/rag/retrieval.py  # 向量检索
└── src/backend/agentchat/services/storage/      # 对象存储
```

---

## 9. 关键模块说明

### 9.1 Agent系统

**位置**: `src/backend/agentchat/core/agents/`

**核心文件**:
- `base_agent.py`: Agent基类，定义统一接口
- `react_agent.py`: ReAct模式Agent
- `plan_agent.py`: 计划-执行Agent
- `mcp_agent.py`: MCP协议Agent

**使用示例**:
```python
from agentchat.core.agents import ReactAgent
from agentchat.core.models import get_llm

# 创建Agent
llm = get_llm("conversation_model")
agent = ReactAgent(
    model=llm,
    system_prompt="你是一个有用的AI助手",
    tools=[search_tool, calculator_tool]
)

# 执行Agent
response = await agent.run("今天天气怎么样？")
```

### 9.2 RAG系统

**位置**: `src/backend/agentchat/services/rag/`

**核心文件**:
- `retrieval.py`: 检索服务
- `parser.py`: 文档解析
- `chunking.py`: 分块策略
- `handler.py`: RAG处理器

**使用示例**:
```python
from agentchat.services.rag.handler import RAGHandler

# 上传文档
await RAGHandler.upload_document(
    knowledge_id="kb_xxx",
    file_path="document.pdf"
)

# 检索
docs = await RAGHandler.retrieve(
    query="什么是AI？",
    knowledge_ids=["kb_xxx"],
    top_k=5
)
```

### 9.3 MCP系统

**位置**: `src/backend/agentchat/services/mcp/`

**核心文件**:
- `manager.py`: MCP服务器管理
- `client.py`: MCP客户端

**配置示例**:
```yaml
mcp_servers:
  system:
    - name: "filesystem"
      command: "npx"
      args: ["-y", "@modelcontextprotocol/server-filesystem", "/path"]
```

---

## 10. 数据流说明

### 10.1 对话流程

```
用户输入
    ↓
[前端] src/pages/Chat/InputArea.vue
    ↓ HTTP POST
[API] src/backend/agentchat/api/v1/completion.py
    ↓
[Service] src/backend/agentchat/services/chat_service.py
    ↓
[Agent] src/backend/agentchat/core/agents/
    ↓
[能力层] RAG/Tools/MCP
    ↓
[模型层] LLM调用
    ↓ SSE Stream
[前端] src/composables/useSSE.ts
    ↓
实时渲染
```

### 10.2 文档处理流程

```
文件上传
    ↓
[API] src/backend/agentchat/api/v1/knowledge.py
    ↓
[Parser] src/backend/agentchat/services/rag/parser.py
    ↓
[Chunking] src/backend/agentchat/services/rag/chunking.py
    ↓
[向量化] Embedding模型
    ↓
[存储] 向量数据库 (ChromaDB/Milvus)
    ↓
完成通知
```

---

**文档维护**: 技术团队  
**最后更新**: 2026-05-14  
**联系方式**: 微信 dtsola（技术交流 | 商务合作）

**变更记录**:
- v1.0 (2026-05-14): 初始版本，完整代码架构文档
