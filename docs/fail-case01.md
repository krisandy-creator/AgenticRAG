# Rerank API 完全正常，且对正确 chunk 的排序完全正确：

  ┌──────┬──────────────────────────────────────┬─────────────┐
  │ 排名 │                 内容                 │ rerank 得分 │
  ├──────┼──────────────────────────────────────┼─────────────┤
  │ [0]  │ 泡沫切断机构…完成切断后…继续推动80mm │ 0.938944    │
  ├──────┼──────────────────────────────────────┼─────────────┤
  │ [1]  │ 泡沫与护角之间的间隙为50~75mm        │ 0.686473    │
  ├──────┼──────────────────────────────────────┼─────────────┤
  │ [2]  │ 贴角机构用于给泡沫四角粘贴护角       │ 0.570781    │
  └──────┴──────────────────────────────────────┴─────────────┘

  ---
  这验证了之前的分析

  rerank API 给出了正确 chunk 0.939 的高分，排第一。问题出在紧随其后的 EvidenceSelector._rank_by_intent：

  combined = 0.45 × 0.939 + 0.55 × 0.032 = 0.422 + 0.018 = 0.44

  rerank 的 0.939 被 intent_score（~0.032）拖到 0.44。 此时若有其他 chunk 在 n-gram 层面与问题更相似（比如 section
  标题含"切断规则"的 chunk，intent_score 可能有 0.2~0.4），它的 combined 分反而更高：

  某个标题含"切断规则"的 chunk: 0.45 × 0.5 + 0.55 × 0.35 = 0.225 + 0.193 = 0.418

  差距仅 0.44 vs 0.42，极其脆弱。如果还有更多这类 chunk，正确 chunk 就会被挤出 top 6。

  结论

  故障链完整闭合：检索命中 → rerank API 正确排第一(0.939) → EvidenceSelector._rank_by_intent 用 n-gram
  词法重叠覆盖分数，把 0.939 拖到 ~0.44 → 可能被挤出 top 6 → LLM 收不到含 80mm 的证据 → 输出"证据不足"。