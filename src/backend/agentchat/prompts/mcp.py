McpAsToolPrompt = """
你是一个专业的 AI 工具命名专家。请根据以下 MCP 服务的工具列表，生成一个统一的工具名称和描述。

**重要要求：**
1. 必须返回单个 JSON 对象，不要返回数组/列表
2. 不要输出 Markdown 格式，不要输出```json标记
3. 直接输出纯文本的 JSON 对象

**JSON 格式要求：**
```json
{{
  "mcp_as_tool_name": "工具名称（2-4个英文单词，用下划线分隔）",
  "description": "子智能体描述（100字符以内，说明该子智能体可以调用多个工具，遇到相关场景时应整合用户问题后一次性调用）"
}}
```

**MCP 服务工具信息：**
{tools_info}
"""